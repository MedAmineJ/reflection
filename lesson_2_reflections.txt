What happens when you initialize a repository? Why do you need to do it?
creation of a file called .git


How is the staging area different from the working directory and the repository? What value do you think it offers?
it contains only the files that we added

How can you use the staging area to make sure you have one commit per logical change?
you can make your changes then add them to the staging area and when your logical change is done you can add your new commit using git commit and saving the new form of your files in a new Repository

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
Braches helps you to create an other version of ur project while keeping the master version unchanged

How do the diagrams help you visualize the branch structure?
you can see which commits are reachable from a certain commit and it helps see if the changes you made in one branch are present in other branches

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
the resault is a mix of both commits containing files created in both commits but not changes deleted in one commit and not the other cause it will make the programme buggy. we present it the way we do to know the files parents ...

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?
sometimes if git chooses how to merge it will creat a bug but sometimes it s so simple that git can make it alone so it makes it

